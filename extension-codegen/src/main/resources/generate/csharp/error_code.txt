${generateTime}
${iohaoHome}
using System.Collections.Generic;
using IoGame.Sdk;

namespace ${namespace}
{
    /// <summary>
    /// GameCode, ErrorCode.
    /// </summary>
    /// <remarks>Author: https://github.com/iohao/ionet</remarks>
    public static class GameCode
    {
    <%for(o in errorCodeDocumentList) {%>
        /// <summary>
        /// ${o.description}
        /// </summary>
        public static readonly int ${o.name} = CmdKit.MappingErrorCode(${o.value}, "${o.description!}");

    <%}%>
        public static void Init() {
            // trigger errorCodeMapping init.
            // Call the method once to trigger the initialization of the error code.
            var ints = new List<int>
            {
            <%for(o in errorCodeDocumentList) {%>
                ${o.name},
            <%}%>0
            };
        }
    }
}